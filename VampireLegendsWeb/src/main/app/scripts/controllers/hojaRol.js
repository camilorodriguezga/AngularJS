'use strict';

/**
 * @ngdoc function
 * @name vampireLegends.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the proyecto1App
 */
angular.module('vampireLegends')
  .controller('HojaRolCtrl', ['$scope', '$q', function ($scope, $q) {
	
  }]);
