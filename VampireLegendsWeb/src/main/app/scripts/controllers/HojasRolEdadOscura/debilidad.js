'use strict';

/**
 * @ngdoc function
 * @name vampireLegends.controller:SaludCtrl
 * @description
 * # SaludCtrl
 * Controller of the vampireLegends
 */
angular.module('vampireLegends')
    .controller('DebilidadCtrl', ['$scope', '$q', function ($scope, $q) {

    }]);
