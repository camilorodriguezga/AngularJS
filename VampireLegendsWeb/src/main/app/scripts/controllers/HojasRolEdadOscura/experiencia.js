'use strict';

/**
 * @ngdoc function
 * @name vampireLegends.controller:ExperienciaCtrl
 * @description
 * # ExperienciaCtrl
 * Controller of the vampireLegends
 */
angular.module('vampireLegends')
  .controller('ExperienciaCtrl', ['$scope', '$q', function ($scope, $q) {
	
  }]);
