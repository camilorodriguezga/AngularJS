'use strict';

/**
 * @ngdoc function
 * @name vampireLegends.controller:HabilidadesCtrl
 * @description
 * # HabilidadesCtrl
 * Controller of the vampireLegends
 */
angular.module('vampireLegends')
  .controller('HabilidadesCtrl', ['$scope', '$q', function ($scope, $q) {
	
  }]);
