'use strict';

/**
 * @ngdoc function
 * @name vampireLegends.controller:AtributosCtrl
 * @description
 * # AtributosCtrl
 * Controller of the vampireLegends
 */
angular.module('vampireLegends')
  .controller('AtributosCtrl', ['$scope', '$q', function ($scope, $q) {
	
	  
  }]);
